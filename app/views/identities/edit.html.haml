- if @identities.empty?
  You have no identities
- else
  Your identities
%ul
  - @identities.each do |identity|
    %li= link_to identity.name, identity
= link_to "Add an identity", new_identities_path
